/**
 * IAM module constants
 */

/**
 * Injection token for IAM options
 */
export const IAM_OPTIONS = 'IAM_OPTIONS';

/**
 * Injection token for current user
 */
export const CURRENT_USER = 'CURRENT_USER';

/**
 * Metadata key for roles
 */
export const ROLES_KEY = 'roles';

/**
 * Metadata key for permissions
 */
export const PERMISSIONS_KEY = 'permissions';

/**
 * Metadata key for public routes (no auth required)
 */
export const PUBLIC_ROUTE_KEY = 'isPublic';
